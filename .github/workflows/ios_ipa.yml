name: iOS IPA (Release)

on:
  workflow_dispatch:

jobs:
  build-ipa:
    runs-on: macos-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: false

      - name: Clean Flutter cache
        run: |
          flutter clean
          rm -rf ~/.pub-cache/hosted/pub.dev/google_fonts-* || true
          flutter pub cache repair

      - name: Install Flutter dependencies
        run: flutter pub get

      - name: Patch google_fonts for Flutter 3.27+ compatibility
        run: |
          echo "Patching google_fonts to fix const evaluation error..."

          GOOGLE_FONTS_PATH=$(find ~/.pub-cache/hosted/pub.dev -name "google_fonts-*" -type d | head -n 1)

          if [ -z "$GOOGLE_FONTS_PATH" ]; then
            echo "Error: google_fonts package not found"
            exit 1
          fi

          echo "Found google_fonts at: $GOOGLE_FONTS_PATH"

          VARIANT_FILE="$GOOGLE_FONTS_PATH/lib/src/google_fonts_variant.dart"

          if [ ! -f "$VARIANT_FILE" ]; then
            echo "Error: google_fonts_variant.dart not found at $VARIANT_FILE"
            exit 1
          fi

          echo "Patching $VARIANT_FILE..."

          cp "$VARIANT_FILE" "$VARIANT_FILE.backup"

          sed -i '' 's/^const _fontWeightToFilenameWeightParts = {$/final _fontWeightToFilenameWeightParts = {/' "$VARIANT_FILE"
          sed -i '' 's/^const _fontStyleToFilenameStyleParts = {$/final _fontStyleToFilenameStyleParts = {/' "$VARIANT_FILE"

          if grep -q "^final _fontWeightToFilenameWeightParts = {" "$VARIANT_FILE"; then
            echo "Successfully patched _fontWeightToFilenameWeightParts"
          else
            echo "Failed to patch _fontWeightToFilenameWeightParts"
            sed -n '148,152p' "$VARIANT_FILE"
            exit 1
          fi

          if grep -q "^final _fontStyleToFilenameStyleParts = {" "$VARIANT_FILE"; then
            echo "Successfully patched _fontStyleToFilenameStyleParts"
          else
            echo "Warning: _fontStyleToFilenameStyleParts not found (may not exist in this version)"
          fi

          echo "google_fonts patching completed"

      - name: Verify google_fonts version
        run: |
          echo "Checking google_fonts version..."
          flutter pub deps | grep google_fonts || true

      - name: Import code signing certificate and provisioning profile
        env:
          IOS_CERTIFICATE_BASE64: ${{ secrets.IOS_CERTIFICATE_BASE64 }}
          IOS_CERTIFICATE_PASSWORD: ${{ secrets.IOS_CERTIFICATE_PASSWORD }}
          IOS_PROVISIONING_PROFILE_BASE64: ${{ secrets.IOS_PROVISIONING_PROFILE_BASE64 }}
          KEYCHAIN_PASSWORD: ${{ secrets.IOS_KEYCHAIN_PASSWORD }}
        run: |
          CERT_PATH="$RUNNER_TEMP/ios_distribution.p12"
          PROFILE_PATH="$RUNNER_TEMP/profile.mobileprovision"
          KEYCHAIN_PATH="$RUNNER_TEMP/app-signing.keychain-db"

          echo "$IOS_CERTIFICATE_BASE64" | base64 --decode > "$CERT_PATH"
          echo "$IOS_PROVISIONING_PROFILE_BASE64" | base64 --decode > "$PROFILE_PATH"

          security create-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"
          security set-keychain-settings -lut 21600 "$KEYCHAIN_PATH"
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"

          security import "$CERT_PATH" -P "$IOS_CERTIFICATE_PASSWORD" -A -t cert -f pkcs12 -k "$KEYCHAIN_PATH"
          security set-key-partition-list -S apple-tool:,apple:,codesign: -s -k "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"

          security list-keychain -d user -s "$KEYCHAIN_PATH" login.keychain
          security default-keychain -s "$KEYCHAIN_PATH"

          PROFILE_UUID=$(/usr/libexec/PlistBuddy -c 'Print :UUID' /dev/stdin <<< "$(security cms -D -i "$PROFILE_PATH")")
          PROFILE_NAME=$(/usr/libexec/PlistBuddy -c 'Print :Name' /dev/stdin <<< "$(security cms -D -i "$PROFILE_PATH")")

          mkdir -p ~/Library/MobileDevice/Provisioning\ Profiles
          cp "$PROFILE_PATH" ~/Library/MobileDevice/Provisioning\ Profiles/"$PROFILE_UUID".mobileprovision

          echo "PROFILE_NAME=$PROFILE_NAME" >> "$GITHUB_ENV"
          echo "KEYCHAIN_PATH=$KEYCHAIN_PATH" >> "$GITHUB_ENV"
          echo "PROFILE_UUID=$PROFILE_UUID" >> "$GITHUB_ENV"

          echo "Certificate and provisioning profile imported"
          echo "Profile name: $PROFILE_NAME"
          echo "Profile UUID: $PROFILE_UUID"

          security find-identity -v -p codesigning "$KEYCHAIN_PATH"

      - name: Update Podfile for iOS 15
        run: |
          cp ios/Podfile ios/Podfile.original

          sed -i '' "s/platform :ios, '[0-9.]*'/platform :ios, '15.0'/" ios/Podfile

          sed -i '' '/^post_install do |installer|/,/^end$/d' ios/Podfile

          # Disable code signing on ALL pod targets.
          # Third-party frameworks (Firebase, Sentry, etc.) are binary XCFrameworks
          # and cannot have provisioning profiles assigned to them. Setting
          # CODE_SIGNING_ALLOWED=NO here prevents xcodebuild from even attempting
          # to sign them, which would otherwise cause "does not support provisioning
          # profiles" errors at both archive and export time.
          cat >> ios/Podfile << 'PODFILE_EOF'

post_install do |installer|
  installer.pods_project.targets.each do |target|
    flutter_additional_ios_build_settings(target)
    target.build_configurations.each do |config|
      config.build_settings['IPHONEOS_DEPLOYMENT_TARGET'] = '15.0'
      config.build_settings['CODE_SIGNING_REQUIRED']      = 'NO'
      config.build_settings['CODE_SIGNING_ALLOWED']       = 'NO'
      config.build_settings['CODE_SIGN_IDENTITY']         = ''
    end
  end
end
PODFILE_EOF

          echo "Podfile updated"
          tail -20 ios/Podfile

      - name: Install CocoaPods dependencies
        run: |
          cd ios
          pod install
          cd ..
          echo "Pods installed"

      - name: Flutter build iOS (no codesign)
        run: flutter build ios --release --no-codesign

      # ─── Archive ────────────────────────────────────────────────────────────
      # We sign ONLY the Runner app target here. Pod frameworks were built with
      # CODE_SIGNING_ALLOWED=NO (set in the Podfile post_install hook above), so
      # xcodebuild will skip them. Passing the same flags on the command line
      # re-confirms the intent and prevents Xcode from overriding the Podfile
      # settings for the pods during the archive phase.
      - name: Archive with Xcode (manual signing)
        env:
          IOS_KEYCHAIN_PASSWORD: ${{ secrets.IOS_KEYCHAIN_PASSWORD }}
          IOS_TEAM_ID: ${{ secrets.IOS_TEAM_ID }}
          IOS_BUNDLE_ID: ${{ secrets.IOS_BUNDLE_ID }}
        run: |
          echo "Creating Xcode archive..."
          echo "Bundle ID : ${IOS_BUNDLE_ID}"
          echo "Profile   : ${PROFILE_NAME}"
          echo "Team      : ${IOS_TEAM_ID}"

          security unlock-keychain -p "$IOS_KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"

          xcodebuild \
            -workspace ios/Runner.xcworkspace \
            -scheme Runner \
            -configuration Release \
            -sdk iphoneos \
            -destination 'generic/platform=iOS' \
            -archivePath build-output/app.xcarchive \
            PRODUCT_BUNDLE_IDENTIFIER="${IOS_BUNDLE_ID}" \
            PROVISIONING_PROFILE_SPECIFIER="${PROFILE_NAME}" \
            CODE_SIGN_STYLE=Manual \
            DEVELOPMENT_TEAM="${IOS_TEAM_ID}" \
            CODE_SIGN_IDENTITY="Apple Distribution" \
            OTHER_CODE_SIGN_FLAGS="--keychain ${KEYCHAIN_PATH}" \
            archive 2>&1 | tee build.log

          if [ ! -d "build-output/app.xcarchive" ]; then
            echo "Archive failed! Last 100 lines of build log:"
            tail -100 build.log
            exit 1
          fi

          echo "Archive created successfully"
          ls -lh build-output/app.xcarchive

      # ─── Export ─────────────────────────────────────────────────────────────
      # Key fix: stripSwiftSymbols + manageAppVersionAndBuildNumber are omitted
      # intentionally. The critical change vs. the original is that we add
      # "compileBitcode = false" (bitcode is deprecated and can cause export
      # issues on newer Xcode), and we pass the profile via UUID rather than
      # display name to avoid any ambiguity when multiple profiles are installed.
      - name: Export signed IPA
        env:
          IOS_TEAM_ID: ${{ secrets.IOS_TEAM_ID }}
          IOS_BUNDLE_ID: ${{ secrets.IOS_BUNDLE_ID }}
          IOS_KEYCHAIN_PASSWORD: ${{ secrets.IOS_KEYCHAIN_PASSWORD }}
        run: |
          security unlock-keychain -p "$IOS_KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"

          # Use UUID (not display name) to unambiguously identify the profile.
          # This is the most reliable way to reference a provisioning profile
          # in ExportOptions.plist and avoids "profile not found" edge cases.
          cat > ExportOptions.plist << PLIST_EOF
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>method</key>
  <string>app-store-connect</string>
  <key>teamID</key>
  <string>${IOS_TEAM_ID}</string>
  <key>signingStyle</key>
  <string>manual</string>
  <key>signingCertificate</key>
  <string>Apple Distribution</string>
  <key>compileBitcode</key>
  <false/>
  <key>uploadSymbols</key>
  <true/>
  <key>provisioningProfiles</key>
  <dict>
    <!-- Map ONLY the app bundle ID. Pod frameworks have no bundle IDs
         and must never appear here — that is what caused the original error. -->
    <key>${IOS_BUNDLE_ID}</key>
    <string>${PROFILE_UUID}</string>
  </dict>
</dict>
</plist>
PLIST_EOF

          echo "ExportOptions.plist:"
          cat ExportOptions.plist

          echo "Exporting signed IPA..."
          xcodebuild -exportArchive \
            -archivePath build-output/app.xcarchive \
            -exportPath build-output/ios \
            -exportOptionsPlist ExportOptions.plist \
            2>&1 | tee export.log

          if [ $? -ne 0 ]; then
            echo "Export failed! Full export log:"
            cat export.log
            exit 1
          fi

          IPA_PATH=$(find build-output/ios -name "*.ipa" | head -n 1)
          if [ -z "$IPA_PATH" ]; then
            echo "Error: IPA not found after export"
            exit 1
          fi

          echo "Signed IPA exported: $IPA_PATH"
          ls -lh "$IPA_PATH"

          # Verify codesigning
          mkdir -p /tmp/ipa-check
          unzip -q "$IPA_PATH" -d /tmp/ipa-check
          APP_PATH=$(find /tmp/ipa-check/Payload -name "*.app" -maxdepth 1 | head -n 1)
          codesign -dvvv "$APP_PATH" 2>&1
          rm -rf /tmp/ipa-check

      - name: Upload IPA as artifact
        uses: actions/upload-artifact@v4
        with:
          name: ios-release-ipa
          path: build-output/ios/*.ipa
          retention-days: 30

      - name: Upload to TestFlight
        env:
          APP_STORE_CONNECT_API_KEY_ID: ${{ secrets.IOS_APP_STORE_KEY_ID }}
          APP_STORE_CONNECT_ISSUER_ID: ${{ secrets.IOS_APP_STORE_ISSUER_ID }}
          APP_STORE_CONNECT_API_KEY_BASE64: ${{ secrets.IOS_APP_STORE_KEY_BASE64 }}
        run: |
          mkdir -p ~/.private_keys
          echo "$APP_STORE_CONNECT_API_KEY_BASE64" | base64 --decode \
            > ~/.private_keys/AuthKey_${APP_STORE_CONNECT_API_KEY_ID}.p8

          IPA_PATH=$(find build-output/ios -name "*.ipa" | head -n 1)

          echo "Uploading to TestFlight..."

          # xcrun altool is deprecated in Xcode 15+; use xcrun notarytool or
          # the uploadSymbols path instead. altool is kept here for compatibility
          # but will print a deprecation warning on recent Xcode versions.
          xcrun altool --upload-app \
            --type ios \
            --file "$IPA_PATH" \
            --apiKey "$APP_STORE_CONNECT_API_KEY_ID" \
            --apiIssuer "$APP_STORE_CONNECT_ISSUER_ID" \
            --verbose

          echo "Uploaded to TestFlight successfully"

      - name: Clean up
        if: always()
        run: |
          if [ -n "$KEYCHAIN_PATH" ] && [ -f "$KEYCHAIN_PATH" ]; then
            security delete-keychain "$KEYCHAIN_PATH" 2>/dev/null || true
          fi
          rm -f "$RUNNER_TEMP"/*.p12 2>/dev/null || true
          rm -f "$RUNNER_TEMP"/*.mobileprovision 2>/dev/null || true
          rm -rf ~/.private_keys 2>/dev/null || true
          rm -f ExportOptions.plist 2>/dev/null || true
          rm -f build.log export.log 2>/dev/null || true
          echo "Cleanup done"
